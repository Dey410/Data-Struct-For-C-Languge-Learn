# 学习笔记

## 26/1/17

### 1.算法满足的五个重要特性：

1. 有穷性
2. 确定性
3. 可行性
4. 输入
5. 输出

### 2.算法的效率(怎么算复杂度)

1. 时间复杂度(T(n) = O(f(n)))

   时间的运行的总时间主要和两点有关，执行每条语句的耗时、每条语句的执行频率。

2. 空间复杂度(S(n)=O(f(n)))

   表空间复杂度主要用来描述某个算法对应的应用程序在计算机上执行，除了用来储存代码和数据内存的空间外的额外空间。

## 3.抽象数据类型ADT

​	**ADT**是一种编程概念，用来定义数据的类型及其操作，而不涉及具体的实现细节。它提供了一种数据的逻辑表示与物理实现分离的方法从而使程序更具可维护性和可扩展性。

​	在c语言中，ADT通常通过数据结构和函数的结合实现。结构体用于定义数据类型，而函数用于操作这些数据。通过这种方式，可以**隐藏数据的内部结构，仅仅暴露操作数据的接口**。

## 26/1/20

# 数据结构

### 线性表

​	**线性表的定义与特点：**由n（n>=0）个数据特性相同的有限序列，称为线性表。

​	*其特点是：*

​		1. 存在唯一的一个被称作“第一个”的数据元素；

​		2. 存在唯一的一个被称作“最后一个”的数据元素；

​		3. 除了第一个元素外，结构中的每个数据元素均由一个前驱；

​		4. 除最后一个元素外，结构中每个元素均只有一个后继。

